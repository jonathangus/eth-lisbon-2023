import { Text } from '@nextui-org/react';

const MockedData = () => {
  return (
    <Text blockquote>
      NOTE: this is mocked data and does not reflect on what this token bound
      account owns.
    </Text>
  );
};

export default MockedData;
